spring.application.name=ebank

server.port=9000

# Configuration pour accéder à la bdd
spring.datasource.url=jdbc:mariadb://localhost:3307/ebank?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=secret
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# H2 db config
#spring.datasource.url=jdbc:h2:mem:bank
#spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=create
spring.jpa.defer-datasource-initialization=true

# initialise la bdd a chaque démarrage en exécutant le fichier schema.sql
#spring.sql.init.mode=always
spring.jpa.show-sql=true
